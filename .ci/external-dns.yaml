# CI-only render inputs for external-dns's fail-on-missing-required guards (not packaged;
# outside the chart dir). Real values come from componentValues at install time.
txtOwnerId: ci
credentialsSecretRef:
  name: ci-secret
